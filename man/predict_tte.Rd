% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_tte.R
\name{predict_tte}
\alias{predict_tte}
\title{Predict Event Counts and Dates from ADTTE Data}
\usage{
predict_tte(
  data,
  planned_total_n,
  target_events = NULL,
  target_dates = NULL,
  nsim = 1000,
  assessment_intervals_days = NULL,
  assessment_cut_days = NULL,
  enrollment_model = "exponential",
  event_model = "exponential",
  dropout_model = "exponential",
  fuyears = 1,
  ci_level = 0.95,
  covariates = NULL,
  fixed_parameters = TRUE,
  seed = NULL,
  show_progress = TRUE,
  show_plot = FALSE
)
}
\arguments{
\item{data}{ADTTE data with columns `usubjid`, `trialsdt`, `randdt`, `adt`,
`cutoffdt`, `status`, `aval`, and optional covariates.}

\item{planned_total_n}{Planned total sample size.}

\item{target_events}{Integer vector of target event counts. Can be `NULL`
if `target_dates` is provided.}

\item{target_dates}{Date vector of target prediction dates. Can be `NULL`
if `target_events` is provided.}

\item{nsim}{Number of simulations.}

\item{assessment_intervals_days}{Piecewise tumor assessment intervals (days),
length `k`. Can be `NULL` to disable adjustment.}

\item{assessment_cut_days}{Time points (days since randomization) where interval
changes, length `k - 1`. Can be `NULL` when interval adjustment is disabled
or when `assessment_intervals_days` has length 1.}

\item{enrollment_model}{Distribution for enrollment model.}

\item{event_model}{Distribution for event model.}

\item{dropout_model}{Distribution for dropout model.}

\item{fuyears}{Simulated follow-up years.}

\item{ci_level}{Confidence interval level.}

\item{covariates}{Optional covariates in event/dropout models.}

\item{fixed_parameters}{Use fitted point estimates (`TRUE`) or sample coefficients
from asymptotic distribution (`FALSE`).}

\item{seed}{Optional seed for reproducible simulation.}

\item{show_progress}{Logical; if `TRUE`, show a simulation progress bar.}

\item{show_plot}{Logical; if `TRUE`, display the prediction plot after simulation.}
}
\value{
A list with prediction tables and interactive plots.
}
\description{
Predict Event Counts and Dates from ADTTE Data
}
